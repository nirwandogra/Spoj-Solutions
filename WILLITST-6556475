#include <stdio.h>
#include <math.h>
int main()
{
int s=0; long long int n;long long int bit;int flag=0;
    scanf("%lld",&n);
     int  b;double d;
     
    if(n!=0 && n!=1)
    {
                          flag=1;
                          bit=n-1;
                          while(bit>=1)
                          {
                                  s=bit&1;  
                            //   printf("%d\n",bit&1);   
                                    if(s==0){
                                                 //printf("entered %);  
                                               flag=0; break;}
                                      bit>>=1; 
                          }
     /*                     i=i-1;
         if(a[i]==1)
    {
              for(int b=0;b<i;b++){c                                     }            
                          
                                     }*/
    if (flag==1){printf("TAK");}
    else{printf("NIE");}
    }
    else
   { printf("TAK");}
return 0;
}

